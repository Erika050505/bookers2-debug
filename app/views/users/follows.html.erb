<div class="body">
 <div class="container">
   <div class="row">
       <h1>フォロー一覧</h1>
    <% @user.followings.each do |user| %>
      <table>
      <tr>
        <td>  <%= user.name %> </td>
        <td><%= link_to '詳細', user_path(user) %></td>
      </tr>
      
      </table>
      <% end %>
    </div>
 </div>
</div>